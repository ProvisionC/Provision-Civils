// Shadowed by app/job/[id]/_layout.tsx — Expo Router uses that directory instead.
// This file is kept as a valid module to prevent Metro bundler errors.
import { View } from "react-native";
export default function JobDetailPlaceholder() {
  return null;
}
